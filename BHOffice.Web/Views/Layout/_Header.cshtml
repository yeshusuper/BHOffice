@{
}
<!DOCTYPE html>
<html>
<head lang="en">
    <meta charset="UTF-8">
    <title>@ViewBag.Title</title>
</head>
<body>
    <div>
        <ul>
            <li>
                <a href="@Url.Action("index", "index")" @(Url.IsCurrent("index", "index") ? "class=\"current\"" : String.Empty)></a>
            </li>
        </ul>
    </div>
    @RenderBody()
</body>
</html>
